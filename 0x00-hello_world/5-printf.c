#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: A program that prints exactly with proper
 * grammer, but the outcome is piece of an art
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
